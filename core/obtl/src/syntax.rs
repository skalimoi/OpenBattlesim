pub enum Syntax {
    DEF,
    BEGIN,
    END,
    CONSTRUCT,
    COPY,
    FROM,
    GOTO,
    BEFORE,
    AFTER,
    TO
}

pub enum Group {
    COMMON,
    BODY,
    ACTION,
    LOGIC,
    OPTIONS
}

