pub struct GenData {
    pub index: Vector3i,
    pub temperature: Vec<real>,
    pub altitude: real,
    pub pressure: Vec<real>,
    pub humidity: Vec<real>,
    pub wind: Vec<Vector3>,
    pub td: Vec<real>,
}

impl GenData {
    
}

